var numbers = [12, 40, 70, 49];
// *index is the number of elements, it starts with 0, 1, 2, ......
console.log(numbers[2]);
// *manually setting the index vales
numbers[1]= 30;
console.log(numbers);
// *can also used in reverse like finding the index number from element 

var elementsPostion = numbers.indexOf(49);

console.log(elementsPostion);

